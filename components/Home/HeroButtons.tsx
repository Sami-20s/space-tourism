import React from "react";
import {
    HeroButtonsStyle,
    HeroButtonStyle,
} from "../../styles/HomeStyle/HeroButtons.style";

const HeroButtons = () => {
    return (
        <HeroButtonsStyle>
            <HeroButtonStyle>EXPLORE</HeroButtonStyle>
        </HeroButtonsStyle>
    );
};

export default HeroButtons;
